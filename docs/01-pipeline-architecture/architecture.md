# Pipeline Architecture

**Status:** Not started — planned for Day 1
**Maps to:** Deliverable 1 — 8+ stage pipeline

---

_Content pending. Will define the full stage sequence (checkout → build → test →
compliance gates → security scan → package → deploy-staging → smoke test →
promote → deploy-prod), with `diagrams/` holding the Draw.io source + PNG export
and `stage-details/` holding one spec file per stage._
